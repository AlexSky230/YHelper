import { LongTouchDirective } from './long-touch.directive';

describe('LongTouchDirective', () => {
  it('should create an instance', () => {
    const directive = new LongTouchDirective();
    expect(directive).toBeTruthy();
  });
});
